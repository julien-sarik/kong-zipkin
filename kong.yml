_format_version: "3.0"
_transform: true

plugins:
- name: zipkin
  service: dummy-back-end
  config:
    http_endpoint: http://zipkin:9411/api/v2/spans
    sample_ratio: 0.99

services:
- host: httpbin.org
  name: dummy-back-end
  port: 80
  protocol: http
  routes:
  - name: foo
    paths:
    - /foo
    strip_path: true
